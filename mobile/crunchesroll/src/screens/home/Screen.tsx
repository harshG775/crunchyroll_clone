import { Text, ScrollView } from "react-native";
import React from "react";

export default function Home() {
    return (
        <ScrollView>
            <Text>Screen</Text>
        </ScrollView>
    );
}
